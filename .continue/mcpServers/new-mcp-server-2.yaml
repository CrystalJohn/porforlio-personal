name: GitHub
version: 1.0.0
schema: v1
mcpServers:
  - name: github
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-github"
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: <your-github-pat>
