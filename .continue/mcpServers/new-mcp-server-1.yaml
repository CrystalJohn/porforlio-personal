name: Filesystem
version: 1.0.0
schema: v1
mcpServers:
  - name: filesystem
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - "d:/AI Dev Daily Workflow"
      - "C:/Users/tranp/.continue"
    env: {}
